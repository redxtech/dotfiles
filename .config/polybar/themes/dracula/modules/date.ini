[module/date]
type = internal/date
interval = 1

time = "%H:%M"
time-alt = "%a, %b %d %H:%M:%S"

format = <label>
format-prefix = 󰅐
format-prefix-background = ${colours.date}
format-prefix-foreground = ${colors.bg}
format-prefix-padding = 1
format-underline = ${colours.date}
label = %time%
label-background = ${colors.bg-alt}
label-foreground = ${colors.fg}
label-padding = 1
