[module/cpu]
type = internal/cpu
interval = 1

format = <label>%{A}
format-prefix = %{A1:bspc rule -a kitty -o state=floating; kitty -e btop:}󰍛
format-prefix-background = ${colours.cpu}
format-prefix-foreground = ${colors.bg}
format-prefix-padding = 1
format-underline = ${colours.cpu}
label = %percentage:2%%
label-background = ${colors.bg-alt}
label-foreground = ${colors.fg}
label-padding = 1

