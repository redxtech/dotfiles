[module/memory]
type = internal/memory
interval = 5

format = <label>%{A}
format-prefix = %{A1:bspc rule -a kitty -o state=floating; kitty -e btop:}%{T7}󰘚%{T-}
format-prefix-background = ${colours.memory}
format-prefix-foreground = ${colors.bg}
format-prefix-padding = 1
format-underline = ${colours.memory}
label = %{T2}%used%%{T-}
label-background = ${colors.bg-alt}
label-foreground = ${colors.fg}
label-padding = 1
