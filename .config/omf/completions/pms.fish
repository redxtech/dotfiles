# completions for pms function
complete -c pms -d "show pms status"
complete -c pms -f -a "start" -d "start pms"
complete -c pms -f -a "restart" -d "restart pms"
complete -c pms -f -a "stop" -d "stop pms"
complete -c pms -f -a "status" -d "show pms status"
complete -c pms -f -a "enable" -d "enable and start pms"
complete -c pms -f -a "disable" -d "disable and stop pms"

