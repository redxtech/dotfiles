# Pre install scripts for arch

echo arch pre install scripts

# install pacaur
#mkdir pacaur-install
#cd pacaur-install

# grab pacaur pkgbuild and install it
#curl -o PKGBUILD https://aur.archlinux.org/cgit/aur.git/plain/PKGBUILD?h=pacaur
#makepkg PKGBUILD
#sudo pacman -U pacaur*.tar.xz --noconfirm
#cd ..
#rm pacaur-install

# make initial platform specific pages list
PACKAGES="dolphin dolphin-megasync-git "
